// exportando interface de dados
export interface IDeleteSoccerAdminDTO {
  userId: string;
  id: string;
}

// exportando interface de dados
export interface IDeleteSoccerOwnerDTO {
  userId: string;
}